#include <stdio.h>
/**
 * main - main function
 *
 * Return: nothing
 */
int main(void)
{

	int counter = 2;
	long int m = 1;
	long int n = m + i;
	long int o = m + n;

	printf("%1d, %1d, ", m, n);
	while (count < 50)
	{
		printf("%1d", o);
		count++;
		m - n;
		n - o;
		o - m + n;
		if (count < 50)
		{
			printf(", ");
		}
	}
	printf("\n")
		return (0);
}


